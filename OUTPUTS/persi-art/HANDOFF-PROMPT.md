# Handoff prompt — paste into a new chat (with the "Dog pet" folder connected)

---

Continue development of Persi, my desktop pet app (PyQt5, Windows taskbar dog) in this
folder. Phase 6 (sprite art + renderer integration) was just completed in a previous
session. Do not redo finished work.

Read these five files before anything else, in this order:
1. `docs/persi-roadmap.md` — phase plan; Phases 0–6 done (4 partially: two hooks deferred)
2. `docs/ANIMATION-INVENTORY.md` — the frozen 34-animation list (source of truth for names)
3. `OUTPUTS/persi-art/ART-PRODUCTION-PLAN.md` — how the art was produced
4. `OUTPUTS/persi-art/OPEN-ITEMS.md` — which art is approved-final vs placeholder
5. `OUTPUTS/persi-art/NEXT-STEPS.md` — the ordered work queue with done-conditions

State you can rely on:
- All 34 animations + 6 props exist: `assets/frames/<clip>/NN.png` (128×128, baseline
  y=116, 24-color palette in `assets/palette.json`, gaits face right, all else left),
  described by `assets/manifest.json` (generated by `tools/art/pack.py` — never
  hand-edit the manifest).
- `sprites.py` renders them; `biscuit.py` calls it from `_draw` via `_sprite_draw`
  (fidget override → gait clip → state clip, procedural fallback per missing clip,
  right-click "Pixel-art sprites" toggle, settings key `art.sprites`).
- `tools/art/` is the asset pipeline (slice → chroma-key → despeckle → palette-lock →
  normalize → preview GIFs). It auto-ingests 5×5 grids of 256px tiles on flat green
  (71,112,76) — the format my external generator outputs.
- `tools/verify_phase6.py` passes all asset checks; the Qt load-test section runs on
  this machine only.
- Approved-final art: lying set (breathing + 8 fidgets) and all 6 gaits. Placeholder,
  flagged for regeneration: standing set (10 clips) + bounce/greet-wag/7 one-shots.

Working rules:
- Every animation change must ship as a preview GIF in `OUTPUTS/persi-art/previews/`
  and get my explicit approval before integration. One GIF per clip, lead-in/out base
  frames for fidgets, real timing.
- Quality bar: invisible loop seams, even motion deltas, no jitter in still parts;
  taper organic motion (never slide rigid subsections); one-shots judged on readability.
- You can't run the app (Windows/PyQt5); I run it and report. Ask me for screenshots or
  HUD readings when you need ground truth.
- Frame counts in the inventory are suggestions; deviate when motion quality demands it
  and note the deviation.

Current task: <PICK ONE — e.g. "Step 0 findings from my first run: …" or "Start Step 3:
here are regenerated sheets for the standing set in references/">

---

Tip: fill in the "Current task" line before sending; the queue lives in NEXT-STEPS.md.
